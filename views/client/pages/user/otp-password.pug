extends ../../layouts/default.pug
include ../../mixins/alert.pug

block main 
    +alert-error(3000)
    .container.my-5 
        .row.justify-content-center
            .col-8 
                h1(class="text-center") Xác thực
                
                form(
                    method="POST" 
                    action=`/user/password/otp`
                )
                   
                    div(class='form-group') 
                        label(for='email') Email
                        input(
                            type='email' 
                            class='form-control'
                            id='email'
                            name='email' 
                            value=email 
                            readonly
                        )

                    div(class='form-group') 
                        label(for='otp') Mã OTP
                        input(
                            type='text' 
                            class='form-control'
                            id='otp'
                            name='otp' 
                            required 
                        )

                    div(class='form-group')
                        button(
                            type='submit' 
                            class='btn btn-primary btn-block d-block mx-auto'
                        ) Gửi mã xác thực